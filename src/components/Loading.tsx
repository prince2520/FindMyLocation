import Lottie from "lottie-react";

import LocationLoadingAnimation from '../animation/LottieFiles/Loading.json'

const Loading = () => {
    return (
        <Lottie animationData={LocationLoadingAnimation}/>
    );
};

export default Loading;